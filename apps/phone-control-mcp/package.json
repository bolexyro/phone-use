{
  "name": "@dhd/phone-control-mcp",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @dhd/phone-control build && vitest run --config vitest.config.ts",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "dependencies": {
    "@dhd/phone-control": "workspace:*",
    "@dhd/screenshot-markers": "workspace:*",
    "@modelcontextprotocol/sdk": "1.29.0",
    "zod": "3.25.76"
  }
}
